apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
spec:
  template:
    spec:
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: redis-data
      containers:
      - name: redis
        volumeMounts:
          - name: data
            mountPath: /data
