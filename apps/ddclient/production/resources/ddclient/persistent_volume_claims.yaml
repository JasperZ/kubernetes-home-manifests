apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ddclient-config
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  volumeName: ddclient-config
