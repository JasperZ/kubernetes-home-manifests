resources:
  - resources/bitfinex-crawler/deployment.yaml
  - resources/influxdb/deployment.yaml
  - resources/influxdb/service.yaml
patchesStrategicMerge:
  - patches/add_influxdb.yaml
vars:
  - name: INFLUXDB_SERVICE
    objref:
      kind: Service
      name: influxdb
      apiVersion: v1
