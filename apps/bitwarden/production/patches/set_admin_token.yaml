apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitwarden
spec:
  template:
    spec:
      containers:
      - name: bitwarden
        env:
        - name: ADMIN_TOKEN
          valueFrom:
            secretKeyRef:
              name: bucket
              key: ADMIN_TOKEN
