---

apiVersion: v1
kind: Service
metadata:
  name: nextcloud-collabora
  labels:
    app: nextcloud
    component: collabora
    tier: frontend
spec:
  selector:
    app: nextcloud
    component: collabora
    tier: frontend
  ports:
  - protocol: TCP
    port: 443
    targetPort: 9980
---

apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: nextcloud-collabora
  labels:
    app: nextcloud
    component: collabora
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nextcloud
      component: collabora
      tier: frontend
  template:
    metadata:
      labels:
        app: nextcloud
        component: collabora
        tier: frontend
    spec:
      containers:
      - name: nextcloud-collabora
        image: collabora/code:3.2.2.1
        env:
        - name: domain
          value: "nextcloud\\.jz-c\\.org|test\\.jz-c\\.org"
        ports:
        - containerPort: 9980
        resources:
          limits:
            cpu: "250m"
            memory: "256Mi"
          requests:
            cpu: "125m"
            memory: "128Mi"
