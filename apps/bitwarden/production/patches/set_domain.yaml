apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitwarden
spec:
  template:
    spec:
      containers:
      - name: bitwarden
        env:
        - name: DOMAIN
          value: "https://$(BITWARDEN_INGRESS_DOMAIN)"
